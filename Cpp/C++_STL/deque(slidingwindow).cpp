#include<bits/stdc++.h>
using namespace std;

int main(){
    int n;
    int a[10000];
    int k;
    cin>>n;
    for(int i = 0; i < n; i++){
        cin>>a[i]; 
    }
    cin>>k;

    //we have to access first k elements separately
    deque<int> Q(k);
    for(int i = 0; i < k; i++){
        
    }
    return 0;
}